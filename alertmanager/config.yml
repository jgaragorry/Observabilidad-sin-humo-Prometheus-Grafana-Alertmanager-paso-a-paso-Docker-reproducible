global:
  resolve_timeout: 1m

route:
  receiver: 'default'

receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://localhost:5001'  # Puedes cambiar esto por una integración real

